require 'spec_helper'

describe Rspec::ValidatesTimeliness do
  it 'has a version number' do
    expect(Rspec::ValidatesTimeliness::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
